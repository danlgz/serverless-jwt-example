// tests for <%= functionName %>
// Generated by serverless-mocha-plugin
import { chai, getWrapper } from 'serverless-mocha-plugin';
import * as AWS from 'aws-sdk-mock';
import * as AWS_SDK from 'aws-sdk';

AWS.setSDKInstance(AWS_SDK);

const wrapped = getWrapper('<%= functionName %>', '/<%= functionPath %>', '<%= handlerName %>');

describe('lambda "<%= functionName %>"', () => {
  it('lambda response', async () => {
    const response = await wrapped.run({});
    chai.expect(response).to.not.be.empty;
  });
});
